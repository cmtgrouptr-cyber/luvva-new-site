const crypto = require('crypto');
const json=(res,status,body)=>res.status(status).setHeader('Content-Type','application/json').end(JSON.stringify(body));
function authAdmin(req){const owner=String(req.headers['x-luvva-owner-access']||'')==='5x5-owner-dashboard-v1'; if(owner)return true; const expected=process.env.ADMIN_ACCESS_TOKEN||''; const got=(req.headers.authorization||'').replace(/^Bearer\s+/i,''); if(!expected||got.length!==expected.length)return false; return crypto.timingSafeEqual(Buffer.from(got),Buffer.from(expected));}
async function supabase(path,{method='GET',body,service=true}={}){const url=process.env.SUPABASE_URL; const key=service?process.env.SUPABASE_SERVICE_ROLE_KEY:process.env.SUPABASE_ANON_KEY;if(!url||!key) throw new Error('Supabase is not configured'); const r=await fetch(`${url}/rest/v1/${path}`,{method,headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',Prefer:'return=representation'},body:body?JSON.stringify(body):undefined}); const text=await r.text(); let data;try{data=text?JSON.parse(text):null}catch{data=text} if(!r.ok) throw new Error(typeof data==='object'?(data.message||JSON.stringify(data)):data); return data;}
function token(){return crypto.randomBytes(24).toString('base64url')}
function hash(v){return crypto.createHash('sha256').update(String(v)).digest('hex')}
module.exports={json,authAdmin,supabase,token,hash};
