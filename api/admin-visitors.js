const {json,authAdmin,supabase}=require('./_shared');
module.exports=async(req,res)=>{if(!authAdmin(req))return json(res,401,{ok:false,message:'Unauthorized'});try{const data=await supabase('dashboard_visitors?select=*&order=last_seen_at.desc&limit=500');const contacts=await supabase('contact_submissions?select=*&order=submitted_at.desc&limit=1000');const latest={};for(const c of (contacts||[])){if(c.visitor_id&&!latest[c.visitor_id])latest[c.visitor_id]=c;}const rows=(data||[]).map(r=>({...r,contact_details:latest[r.visitor_id]||null}));return json(res,200,{ok:true,rows});}catch(e){return json(res,503,{ok:false,message:e.message,rows:[]})}};
