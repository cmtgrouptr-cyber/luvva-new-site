LUVVA V18 — Dashboard direct-login route

- Dashboard hidden five-tap control remains unchanged.
- Dashboard modal shows Login instead of generating QR.
- Login now uses a relative direct static route: ./dashboard.html?luvva_route_test=1&route=v18
- The route-test dashboard bypasses owner QR/code authentication only for this diagnostic build and loads preview rows.
- Website hidden-key QR flow is unchanged.
- No gateway visual design changes.
